export interface Asistencia {
  id?: string;  // Asegúrate de que el campo 'id' esté disponible
  alumnoNombre: string;
  alumnoApellido: string;
  asignaturaNombre: string;
  fecha: string;
  hora: string;
}
